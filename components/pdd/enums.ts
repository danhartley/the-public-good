export enum AssistedTechnology {
    Unknown = 1,
    ScreenReader = 2
}

export enum DashboardStyle {
    Tabular = 1,
    Visual = 2
}

export enum Source {
    Test = 1,
    Live = 2
}

export enum Figure {
    Features = 1,
    Values = 2
}